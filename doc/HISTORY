12.14.2011

1. Remove deps directory, remove the strange system to load these 3 libraries.

12.12.2011

1. Add specials.lisp to collect these config global variables like *log-path*, now 
ext-blog will write these config files in specified directory
2. file-publisher now support multiple directories. ext-blog now will serve static
files in 2 directories: theme and entry static
3. Move some shell scripts to contrib directory

12.9.2011

1. Support start ext-blog in any directories, instead ext-blog root path, A GREAT CHANGE
2. Move file-publisher to deps directory

12.8.2011

1. Remove data/wenquanyi_12ptb.pcf font file, if you want a bigger font than the default, try to download this font file from : 

https://github.com/kevinlynx/klprj/blob/master/ext-blog/data/wenquanyi_12ptb.pcf

12.7.2011

1. Add a nil decorator to adapter to the newest restas.
2. Support Clozure CL now!

12.3.2011

Update some dependent libraries, fix hunchentoot:*access-log-path* compile error.

9.22.2011

Change veryfi-code to generate an expression (+/*).

9.16.2011

Use flexi-streams to make the verify-code generation more reasonable. Now it generate picture in memory and send the memory data to browser directly, instead output it to a file and then read the file.

9.15.2011

Add a simple verify code, to prevent spam comments

8.25.2011

Add an English version README file.

